# TODO

## Achievements: modern auto-scrolling image component
- [ ] Update `src/components/HomePage/Achievements.js`
  - [ ] Refactor into small subcomponents (kept in same file)
  - [ ] Replace old percentage-based translate keyframes with modern infinite marquee (pixel-based)
  - [ ] Add prefers-reduced-motion + pause-on-hover
  - [ ] Add premium edge-fade mask for a modern look
  - [ ] Keep existing Cloudinary image URLs + card visuals
  - [ ] Ensure no layout jank and smooth animation (`will-change`)
- [ ] Run `npm run dev` and visually verify the Achievements section

